# plant_sensor_esp32/main/CMakeLists.txt
idf_component_register(SRCS "main_tcpip.c"
                    INCLUDE_DIRS "."
                    REQUIRES 
                        esp_http_server 
                        nvs_flash 
                        freertos
                        driver
                        esp_timer
                        esp_netif
                        esp_event
                        lwip
                    )

# 禁用警告
target_compile_options(${COMPONENT_LIB} PRIVATE -Wno-error=format -Wno-error=implicit-function-declaration)