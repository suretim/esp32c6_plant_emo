# plant_sensor_esp32/main/CMakeLists.txt i2c_scanner main_as7343.c
idf_component_register(SRCS "main_as7343.c"
                    INCLUDE_DIRS "."
                    REQUIRES 
                        esp_http_server 
                        nvs_flash 
                        freertos
                        driver
                        esp_timer
                        esp_wifi
                        esp_netif
                        esp_event
                    )

# 禁用某些警告
target_compile_options(${COMPONENT_LIB} PRIVATE -Wno-error=format -Wno-error=maybe-uninitialized)